{
  "name": "notion-mongo-sync",
  "version": "1.0.0",
  "description": "A small app to sync a notion table with a mongo database",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@notionhq/client": "^0.2.3",
    "dotenv": "^10.0.0",
    "mongodb": "^4.0.1"
  }
}
